{
  "name": "@compile/cli",
  "private": true,
  "version": "0.0.1",
  "bin": "bin/compile",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@compile/eslint-plugin": "./eslint"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.5",
    "@types/compile": "./types",
    "@types/node": "^20.6.3",
    "commander": "^11.0.0",
    "picocolors": "^1.0.0",
    "rollup": "^3.29.2",
    "typescript": "^5.0.2"
  },
  "config": {
    "compile": {
      "java": true
    }
  }
}
